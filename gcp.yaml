plugin: google.cloud.gcp_compute  # Uses GCP compute plugin for dynamic inventory
projects:
  - your-project-id               # Specifies which GCP project to scan
auth_kind: serviceaccount         # Authentication method
service_account_file: /path/to/service-account.json  # Credentials location